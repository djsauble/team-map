var FilterDropdown = Backbone.View.extend({
  className: "filter-bar__dropdown",
  tagName: "select",

  initialize: function() {
    this.render();
  },

  render: function() {
    // Append <option> elements
  }
});
